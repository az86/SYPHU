<UserControl x:Class="SYPHU.Views.WizardControls.Types"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <GroupBox Margin="0 2 0 0">
            <Grid>
            	<Grid.RowDefinitions>
            		<RowDefinition/>  
					<RowDefinition/>    	
				</Grid.RowDefinitions>
                <RadioButton Grid.Row="0" Content="定量反应" IsChecked="{Binding IsGradedBtnChecked}" IsEnabled="{Binding IsGradedBtnEnabled}"/>
                <RadioButton Grid.Row="1" Content="定性反应" IsChecked="{Binding IsQuantalBtnChecked}" IsEnabled="{Binding IsQuantalBtnEnabled}"/>
			</Grid>
        </GroupBox>
    </Grid>
</UserControl>

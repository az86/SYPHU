<UserControl x:Class="SYPHU.Views.WizardControls.DataSize"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <GroupBox Grid.RowSpan="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Text="表数 (组数)"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="行数 (重复数)"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="列数 (剂量数)"/>
                <TextBox  Grid.Row="0" Grid.Column="1" Width="50" Text="{Binding PreparationNum}"/>
                <TextBox  Grid.Row="1" Grid.Column="1" Width="50" Text="{Binding ReplicateNum}"/>
                <TextBox  Grid.Row="2" Grid.Column="1" Width="50" Text="{Binding DoseNum}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>

<UserControl x:Class="SYPHU.Views.Client"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" FontSize="12">
    <Grid x:Name="grid">
        <ScrollViewer Grid.Row="1">
            <WrapPanel Name="visualElements">
                <Border Width="{Binding ActualWidth, ElementName=grid, Mode=OneWay}" Height="{Binding ActualHeight, ElementName=grid, Mode=OneWay}">
                	<Border.Background>
                		<LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                			<GradientStop Color="#FF6688AB" Offset="0.022"/>
                			<GradientStop Color="#FFA4C9F1" Offset="0.47"/>
                			<GradientStop Color="#FFF5F8FB" Offset="0.924"/>
                			<GradientStop Color="#FF4A89C9" Offset="0.684"/>
                		</LinearGradientBrush>
                	</Border.Background>
                    <Image OpacityMask="Black" Source="Resources\bk.PNG" Margin="150,0,150,0"/>
                </Border>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
